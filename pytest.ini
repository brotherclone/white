[pytest]
asyncio_mode = auto
addopts = -q
testpaths = tests
python_files = test_*.py
markers =
    integration: mark integration tests that require mocks/fixtures or longer runtime
    venv312: mark tests that must run under .venv312 python
filterwarnings =
    ignore::DeprecationWarning
