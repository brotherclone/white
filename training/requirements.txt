# Rainbow Pipeline Training Dependencies
# Requires Python 3.11 (PyTorch 2.1.2 doesn't support 3.13)

# Core ML
torch==2.1.2  # Last version with Intel Mac support
transformers>=4.30.0

# Data
pandas>=2.0.0
pyarrow>=12.0.0
scikit-learn>=1.3.0

# Training utilities
tqdm>=4.65.0
pyyaml>=6.0

# Optional: Experiment tracking
wandb>=0.15.0

# Optional: Mixed precision training (usually included in torch)
# accelerate>=0.20.0